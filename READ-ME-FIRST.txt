TRUSTOS — REDESIGN + "TODAY'S ACTION"
======================================

TWO THINGS IN THIS UPDATE (design + one feature; no DB changes)
---------------------------------------------------------------

1. VISUAL REDESIGN
   - Warm "paper" canvas + a single deep "forest" green accent.
   - Fraunces editorial serif for headlines, Inter for body.
   - Redesigned landing page (required headline/subheading kept).
   - /demo and client journey restyled to match.
   - "TrustOS" home link added to the client journey (fixes the
     "no back button" issue).

2. "TODAY'S ACTION" — the differentiator
   - Vendor dashboard now opens with ONE card: the single most
     important thing to do next, e.g.
        "Sarah Test is waiting on you."
        One-Year Motherhood Journey — Prepare and send the proposal
        [ Prepare and send the proposal -> ]
   - It picks the project where the VENDOR is the responsible party,
     in pipeline order. If everything is waiting on clients, it says
     "You're all caught up."
   - The client journey's "Your next step" is now the matching hero:
     one clear action with an estimated time. No hunting through tabs.

   This is powered by the journey engine (src/lib/journey.ts) that was
   already there and tested — so the "next action" is always derived
   from real project state, never guessed.


DEPLOY (GitHub — just push)
---------------------------
1. GitHub Desktop -> your deploy branch.
2. Show in Explorer -> delete everything EXCEPT the hidden ".git".
3. Copy everything from inside this folder into the repo folder.
4. Commit -> Push. Netlify rebuilds (~2 min).

No database changes. No new env vars. No re-seed.


STILL OPEN (unchanged — honest list)
------------------------------------
- Agara Live streaming steps are milestones, not bespoke screens.
- The deposit step needs Stripe keys to actually take a card.
- Mood board / prep guide / welcome kit are not built.
These are the real next pieces of work, not part of this update.
